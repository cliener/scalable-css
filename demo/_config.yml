# Site settings
title: Scalable CSS
baseurl: "" # the subpath of your site, e.g. /blog
url: "http://localhost:4000/" # the base hostname & protocol for your site, e.g. http://example.com

# Build settings

sass:
    sass_dir: _sass

defaults: 
  -
    scope:
      path: ""
      type: pages
    values:
      layout: "default"

include: ["_pages", "_components", "_script"]
